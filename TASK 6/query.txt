CREATE TABLE  "EBILLRECIPT" 
   (	"C_ID" NUMBER(10,0), 
	"C_NAME" VARCHAR2(25) NOT NULL ENABLE, 
	"NO_OF_ITEMS" NUMBER(20,0) NOT NULL ENABLE, 
	"TOTAL_BILL" NUMBER(20,0) NOT NULL ENABLE, 
	"DISCOUNT_IN_PER" NUMBER(20,0) NOT NULL ENABLE, 
	"NET_BILL" NUMBER(20,0) NOT NULL ENABLE, 
	 CONSTRAINT "PK_CUS_ID" PRIMARY KEY ("C_ID") ENABLE
   ) ;

//SEQUENCER

Create sequence ERECIPT_sequence
start with 1
increment by 1
minvalue 1
maxvalue 10000;

//INSERT QUERY
INSERT INTO EBILLRECIPT(C_ID,C_NAME,NO_OF_ITEMS,TOTAL_BILL,DISCOUNT_IN_PER,NET_BILL) VALUES (EBILLRECIPT_SEQUENCE.NEXTVAL,'XYZ',5,100,50,50);