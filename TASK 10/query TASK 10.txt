CREATE TABLE  "TASKLIST" 
   (	"ID" NUMBER(20,0), 
	"TODOTIME" VARCHAR2(50), 
	"TODODATE" VARCHAR2(80), 
	"TITLE" VARCHAR2(80), 
	"DESCR" CLOB, 
	 CONSTRAINT "TASK_PK" PRIMARY KEY ("ID") ENABLE
   ) ;

///sequencer


 
 CREATE SEQUENCE   "TASK_SEQUENCE"
  MINVALUE 1
 MAXVALUE 10000 
INCREMENT BY 1
